pipeline {
    agent any
    stages {
        stage('install') {
            steps {
                    dir('react-ts'){
                        sh 'npm install'
                    }
            }
        }
    }
}
